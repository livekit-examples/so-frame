# Local virtualenv
.venv/

# Secrets (keep .env.example committed)
.env

# Python
__pycache__/
*.pyc

# Deploy checkpoints: the four the write-up refers to are committed on purpose, everything
# else copied off a training run stays local.
checkpoints/
*.pt

# Frames pulled off the live robot (utils/pull_frames.py); the calibration captures in
# utils/captures/ are committed on purpose, everything else is scratch.
frames/
